(() => { const form=document.getElementById('unlockForm'), input=document.getElementById('accessCode'), error=document.getElementById('error'), emergency=document.getElementById('emergency'); const release=()=>window.mavion.releaseLock(); form.addEventListener('submit',event=>{event.preventDefault();if(input.value.trim()==='Mavion')release();else{error.hidden=false;input.select()}}); document.getElementById('overrideYes').addEventListener('click',release); document.getElementById('overrideNo').addEventListener('click',()=>{emergency.hidden=true;input.focus()}); window.addEventListener('keydown',event=>{if(event.key==='F6'){event.preventDefault();emergency.hidden=false}}); input.focus(); })();
