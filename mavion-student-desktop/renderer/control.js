(() => { const idle=document.getElementById('idleEnabled'); let idleTimer; const activate=()=>{window.clearTimeout(idleTimer);window.mavion.activateLock()}; const arm=()=>{window.clearTimeout(idleTimer);if(idle.checked)idleTimer=window.setTimeout(activate,5*60*1000)}; document.getElementById('lockDesktop').addEventListener('click',activate); document.getElementById('headerLock').addEventListener('click',activate); document.getElementById('testNotification').addEventListener('click',()=>window.mavion.testNotification()); idle.addEventListener('change',arm); ['mousemove','mousedown','keydown','touchstart','scroll'].forEach(name=>window.addEventListener(name,arm,{passive:true})); document.querySelectorAll('.nav').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('.nav,.page').forEach(el=>el.classList.remove('active'));button.classList.add('active');document.getElementById(button.dataset.page).classList.add('active')})); arm(); })();
